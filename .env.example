# 数据库连接
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/chatbot"

# Dify 配置
DIFY_API_URL="https://api.dify.ai/v1/chat-messages"
DIFY_API_KEY="app-xxxxxxxxxxxxx"

# 企微机器人 webhook（直接通知，无需 n8n）
WECOM_WEBHOOK_URL="https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_KEY"

# n8n webhook（可选，部署 n8n 后使用）
N8N_WEBHOOK_URL="https://your-n8n.com/webhook/lead-notify"

# n8n 管理账号（docker-compose 中使用）
N8N_USER=admin
N8N_PASSWORD=admin

# 服务端口
PORT=3001

# Redis 连接（多实例实时推送；未配置则单机内存模式）
REDIS_URL="redis://localhost:6379"

# 每个实例最大 SSE 长连接数
MAX_SSE_CONNECTIONS=500

# Dify 并发调用上限（同一时刻打向 Dify 的请求数）
DIFY_MAX_CONCURRENT=20

# 聊天接口限流（每 IP 每分钟次数）
CHAT_RATE_LIMIT_MESSAGE=60
CHAT_RATE_LIMIT_MUTATION=60
CHAT_RATE_LIMIT_READ=120

# 设为 1 时禁用限流（仅压测/排查时用）
RATE_LIMIT_DISABLED=0
